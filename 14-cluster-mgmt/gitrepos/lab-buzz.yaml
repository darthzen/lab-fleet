# Fleet GitRepo — buzz namespace.
#
# This is the Fleet `GitRepo` CUSTOM RESOURCE (gitrepos.fleet.cattle.io), not a
# git repository: it points Fleet at the existing lab-fleet repo and tells it
# which paths to render. It lives on the RANCHER CONTROLLER
# (192.168.7.148, kubectl context `rancher`), so it is not part of any bundle here.
#
#   kubectl --context rancher apply -f 14-cluster-mgmt/gitrepos/lab-buzz.yaml
#
# One GitRepo per namespace — see gitrepos/README.md.
#
# MED RISK: stateful. 2 Deployments, 2 StatefulSets (bundled Postgres and
# Redis) and 4 Longhorn PVCs. The chart is VENDORED because buzz-0.1.6 is
# published nowhere reachable; it was recovered from the live Helm release
# secret. Verified: renders byte-identical to live, so adoption is a no-op.
#
# PREREQUISITE SECRET: `buzz-relay` — the relay and BOTH subcharts read it.
#
# Do not add a cert-manager annotation for buzz.ash4d.com. TLS terminates
# at a GCP edge proxy, and two clusters issuing for the same host would
# race on the same _acme-challenge TXT record at renewal. See
# 17-buzz/fleet.yaml.
#
# STAGED: this file carries ONLY `17-buzz`, so applying it is a single
# adoption step and stays idempotent. `17-buzz/minio` is deliberately left
# out — see gitrepos/README.md on why a GitRepo file must never hold more
# than one unadopted path. Add the second path after the first is verified
# Ready:
#
#   kubectl --context rancher -n fleet-default patch gitrepo lab-buzz \
#     --type=merge -p '{"spec":{"paths":["17-buzz","17-buzz/minio"]}}'
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: lab-buzz
  namespace: fleet-default
spec:
  repo: https://github.com/darthzen/lab-fleet
  branch: main
  paths:
    - 17-buzz
  # `ash4d-lab` is a bespoke label already on the sdf1 cluster (c-nnzn9). Chosen
  # over management.cattle.io/cluster-name, which Rancher owns and may rewrite.
  # Do NOT reuse site=ash4d — that belongs to the ash4d-site GitRepo and resolves
  # to a different downstream cluster.
  targets:
    - clusterSelector:
        matchLabels:
          ash4d-lab: ""
  # correctDrift OFF until every namespace is adopted and verified. Turning it on
  # makes Fleet revert out-of-band `kubectl edit`s — the eventual goal, but it
  # will fight this cluster's hand-tuning habits (GPU pins, context sizes).
  correctDrift:
    enabled: false
