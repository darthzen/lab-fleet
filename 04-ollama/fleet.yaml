# Fleet bundle — Ollama (Helm). See fleet-wiring-plan Phase 1.
# Version pinned to 1.67.0 to match the repo's current install (README bumped
# 2026-07-18). NOTE: the 2026-07-04 live capture showed chart 1.60.0 — confirm
# the running release is 1.67.0 before enabling this path, or Fleet will upgrade
# on adoption. V100 pin and OLLAMA_* tuning live in values.yaml.
defaultNamespace: ai
helm:
  releaseName: ollama
  repo: https://helm.otwld.com/
  chart: ollama
  version: "1.67.0"
  valuesFiles:
    - values.yaml
  takeOwnership: true
