apiVersion: batch/v1
kind: CronJob
metadata:
  name: k8s-docs-indexer
  namespace: ai
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 7200
      backoffLimit: 2
      template:
        spec:
          containers:
          - env:
            # Embeddings go through ollama-exporter's pass-through proxy (9401),
            # not ollama directly, so batch embed traffic is counted in the
            # exporter's metrics. See 04-ollama/ollama-exporter/.
            - name: OLLAMA_URL
              value: http://ollama-exporter.ai.svc.cluster.local:9401
            - name: MILVUS_URI
              value: http://milvus.ai.svc.cluster.local:19530
            - name: EMBED_MODEL
              value: nomic-embed-text
            - name: EMBED_DIM
              value: '768'
            - name: COLLECTION
              value: k8s_docs
            - name: WORKDIR
              value: /work
            - name: STATE_DIR
              value: /state
            # Locally-built (see Dockerfile), imported straight into containerd
            # on the node — never pushed to a registry, hence pullPolicy Never.
            image: localhost/k8s-docs-indexer:v2
            imagePullPolicy: Never
            name: indexer
            resources:
              limits:
                cpu: '2'
                memory: 4Gi
              requests:
                cpu: 500m
                memory: 1Gi
            volumeMounts:
            - mountPath: /state
              name: state
            - mountPath: /work
              name: work
          restartPolicy: Never
          volumes:
          - name: state
            persistentVolumeClaim:
              claimName: k8s-docs-indexer-state
          - emptyDir:
              sizeLimit: 5Gi
            name: work
  schedule: 0 3 * * 0
  startingDeadlineSeconds: 600
  successfulJobsHistoryLimit: 3
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: k8s-docs-indexer-state
  namespace: ai
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
